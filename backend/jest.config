// jest.config.js

module.exports = {
  verbose: false,
  silent: true,
  // This will only show console.error outputs
  // Remove this line if you want to suppress all console output
  silent: process.env.NODE_ENV !== 'test',
};